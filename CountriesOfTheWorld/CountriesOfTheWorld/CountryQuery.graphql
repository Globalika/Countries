query country($code: ID!) {
    country(code: $code) {
        code
        name
        capital
        continent {
            name
        }
        phone
        currency
        languages {
            __typename
            name
        }
    }
}
